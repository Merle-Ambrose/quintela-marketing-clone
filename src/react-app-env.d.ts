/*
   This file references the Create React App ambient types provided by `react-scripts`.
   It ensures TypeScript picks up CRA-specific global declarations (JSX, asset
   module types, and other environment typings). Keep this file in place unless
   replacing these ambient declarations with your own `.d.ts` files.
*/
/// <reference types="react-scripts" />
